name: deploy-docker

on:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20"

      - name: Remove root ESLint config
        working-directory: ./
        run: rm .eslintrc.json

      - name: Install dependencies
        working-directory: ./backend
        run: npm install

      - name: Run linter
        working-directory: ./backend
        run: npm run linter

      - name: Run tests
        working-directory: ./backend
        run: npm run test
  
  build:
    name: Build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Remove root ESLint config
        working-directory: ./
        run: rm .eslintrc.json

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20"

      - name: Build Docker image frontend
        run: |
          docker build -t ghcr.io/l-inventaire/client/nginx:latest --build-arg SENTRY_AUTH_TOKEN=${{ secrets.SENTRY_AUTH_TOKEN }} frontend
          export FRONTEND_VERSION=$(node -p "require('./frontend/package.json').version")
          docker tag ghcr.io/l-inventaire/client/nginx:latest ghcr.io/l-inventaire/client/nginx:$FRONTEND_VERSION

      - name: Build Docker image backend
        run: |
          docker build -t ghcr.io/l-inventaire/client/node:latest --build-arg SENTRY_AUTH_TOKEN=${{ secrets.SENTRY_AUTH_TOKEN }} . -f docker/node.Dockerfile
          export BACKEND_VERSION=$(node -p "require('./backend/package.json').version")
          docker tag ghcr.io/l-inventaire/client/node:latest ghcr.io/l-inventaire/client/node:$BACKEND_VERSION

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: RomaricMourgues
          password: ${{ secrets.DOCKER_PASSWORD }}
          registry: ghcr.io

      - name: Push Docker image
        run: |
          docker push ghcr.io/l-inventaire/client/nginx:latest
          docker push ghcr.io/l-inventaire/client/node:latest
          export FRONTEND_VERSION=$(node -p "require('./frontend/package.json').version")
          export BACKEND_VERSION=$(node -p "require('./backend/package.json').version")
          docker push ghcr.io/l-inventaire/client/nginx:$FRONTEND_VERSION
          docker push ghcr.io/l-inventaire/client/node:$BACKEND_VERSION
