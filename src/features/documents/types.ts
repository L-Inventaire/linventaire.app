export type SigningSession = {
  recipient_token: string;
  invoice_id: string;
  external_id: string;
  invoice_snapshot: string;
  recipient: string;
  state: string;
  document_url: string;
  signing_url: string;
};
