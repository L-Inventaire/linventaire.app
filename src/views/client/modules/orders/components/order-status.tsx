export const InvoiceStatus = ({
  readonly,
  value,
  onChange,
}: {
  readonly?: boolean;
  value: string;
  onChange: (value: string) => void;
}) => {
  // Order:
  // draft / sent / ordered
  return <>TODO</>;
};
